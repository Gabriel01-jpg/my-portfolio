import { SkillBanner } from "./style"

export const Skill = ({ title }) => {
    return (
        <div>
            <p>{title}</p>
            <SkillBanner>

            </SkillBanner>
        
        </div>

    )
    
}